# Atizzy root-cause findings (2026-08-22)

Live Supabase project: EventVerse, ref `blalvoelllndmbppbkcy`, region eu-west-1.

Production schema confirms `content_likes.target_type` accepts `EVENT`, `SONG`, `ALBUM`, `ARTIST`, `VENUE`, `ORGANIZER`, and `MUSIC_VIDEO`; `MUSIC` is invalid. `content_likes` primary key is `(user_id,target_type,target_id)`. `content_ratings` accepts the same target types and has unique `(user_id,target_type,target_id)`.

Relevant production columns: `songs(id,artist_id,title,duration_seconds,audio_url,cover_url,play_count,status,published_at,music_video_url,lyrics_text)` and `venues(id,owner_id,name,city,address,capacity,description,venue_type,amenities,rules,contact_phone,image_urls,pricing,cancellation_policy,created_at,updated_at)`.

Existing frontend service has `setContentRating`, `setContentLike`, `createArtistSong`, `updateArtistSong`, `setArtistSongStatus`, `createOwnedVenue`, and `updateOwnedVenue`, but no song or venue delete/archive service. Music detail currently passes targetType=`MUSIC`. Root player currently stores only one song and exposes play/toggle; no queue previous/next API is present. Existing Artist workspace already has song create/edit/publish UI but no delete button and currently only title/duration/artwork/audio in the visible form; existing song schema/service supports music video and lyrics.

Source evidence: live Supabase schema inspection via configured Supabase MCP; local files `src/services/user.js` and `src/EventVerse.jsx`.
