# Linked Atizzy Root-Cause Audit and Repair Directive

Source: https://chatgpt.com/s/t_6a874d5ce7308191976020169f71111a

## Root-cause findings from the linked conversation

The reported fabricated artists, events, venues, and songs are likely seeded in Supabase by `supabase/0004_eventverse_catalog_seed.sql`, not only hardcoded in the frontend. The frontend catalog service reads database records, so removing frontend arrays alone does not remove the records that appear in the existing cards.

The Admin Dashboard must use its authoritative administrative RPC/service path rather than ordinary `user_profiles` access. Normal RLS may restrict a user to their own profile, while Super Admin operations such as `admin_list_users(...)` are designed to read the complete directory. The frontend must consistently call the backend authority and preserve least-privilege/RLS boundaries.

Required data flow:

`Supabase seed/data -> RLS -> authoritative admin RPC -> frontend domain service -> existing UI -> deployed application`

## Required repair policy

Do not redesign or remove existing cards, boards, pills, sections, navigation, detail layouts, or visual hierarchy. First identify the actual source of every displayed record. Remove or exclude fabricated seeded/demo content safely, avoiding deletion of records with commerce or operational dependencies. Use exact provenance guards and preserve real user-created records.

Use live catalog services for attendee pages and authoritative admin RPC wrappers for Super Admin directories, governance metrics, applications, moderation, audit, and system data. Maintain explicit loading, empty, error, and success states. Validate the deployed application end-to-end, including Home/discovery, catalog detail paths, Admin/Super Admin visibility, RLS boundaries, and production build/deployment alignment.

Completion requires tests, TypeScript/build validation, live-data verification, production smoke checks, and evidence documenting whether any production cleanup was intentionally avoided because dependent commerce records exist.
