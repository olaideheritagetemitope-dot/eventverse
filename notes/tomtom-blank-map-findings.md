# TomTom blank-map forensic findings

The supplied screenshots show the same failure in both render modes. The venue form's inline map region is a dark, uniformly empty rectangle; the fullscreen modal shows a dark, uniformly empty canvas below the header. The loading state is no longer hanging, so the lifecycle timeout fix is working. There are no visible TomTom tiles, road labels, attribution, controls, or marker. The footer instructions render over the empty map, and the location remains unconfirmed. This points to a basemap/canvas/style/tile rendering failure rather than a search or confirmation UI failure. Both inline and fullscreen modes share the same map surface, so the fix must be in the shared TomTom/MapLibre initialization or tile/style request path, with layout sizing checked as a secondary possibility.
